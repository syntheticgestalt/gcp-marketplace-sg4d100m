# Ignore all files/folders
*
# Don't ignore any files/folders if the parent folder is 'un-ignored'
# This also avoids warnings when adding an already-checked file with an ignored parent.
!/**/
# whitelist
!/docs/**
!/.github/**
!/.envrc
!/.gitattributes
!/.gitignore
!/default.nix
!/justfile
!/pixi.lock
!/pixi.toml
